<%= render 'layouts/sidebar' %>
<div class="content__details">
  <h1>Editing fixed expense</h1>
  <%= render "form", fixed_expense: @fixed_expense %>
  <br>
  <div class="btn">
    <%= link_to "Show this expense", @fixed_expense, class: "btn-success" %> 
    <%= link_to "Back to expenses", fixed_expenses_path, class: "btn-back" %>
  </div>
</div>

