<%= form_with(model: fixed_expense) do |form| %>
  <% if fixed_expense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fixed_expense.errors.count, "error") %> prohibited this fixed_expense from being saved:</h2>

      <ul>
        <% fixed_expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form__content">
    <%= form.label :date, style: "display: block", class: "form__content-label" %>
    <%= form.date_field :date, class: "form__content-field" %>
  </div>

  <div class="form__content">
    <%= form.label :type_of_expense, style: "display: block", class: "form__content-label" %>
    <%= form.text_field :type_of_expense, class: "form__content-field" %>
  </div>

  <div class="form__content">
    <%= form.label :cost, style: "display: block", class: "form__content-label" %>
    <%= form.number_field :cost, class: "form__content-field" %>
  </div>

  <div class="form__submit">
    <%= form.submit class: "btn-success" %>
  </div>
<% end %>
