<%= render 'layouts/sidebar' %>
<div class="content__details">
  <p style="color: green"><%= notice %></p>
  <%= render @expense %>
  <div class="btn">
    <%= link_to "Edit this expense", edit_expense_path(@expense), class: "btn-secondary" %> |
    <%= link_to "Back to expenses", expenses_path, class: "btn-back" %>
    <%= button_to "Destroy this expense", @expense, method: :delete, class: "btn-danger" %>
  </div>
</div>
